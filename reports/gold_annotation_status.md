# Gold annotation status

- 30 dual-image Label Studio tasks rendered
- 13/13 `tests/test_gold_annotation.py` passed
- Full `label-studio` pip install failed on this Mac (`psycopg2-binary` build). XML is well-formed; import after Owner installs LS in `~/.venv-labelstudio`
- Demo gold JSONL: 5 POS + 5 NEG
- No holdout read, no train, no promote
