"""Dataset V3 construction for the L1 ``ma_dense_core`` detector.

Not a layer: this package may import ``yoyo.layers.l1_detection`` (the frozen
renderer lives there), while layers may not import back. See
``tests/test_layer_boundaries.py`` for what the layer rule actually covers.
"""
